package com.skidding.atlas.event.impl.game;

import com.skidding.atlas.event.Event;

public class RunTickEvent extends Event {
    public RunTickEvent(EventType eventType) {
        super(eventType);
    }
}
