package com.skidding.atlas.event.impl.player.action;

import com.skidding.atlas.event.Event;

public class SwingEvent extends Event {
    public SwingEvent(EventType eventType) {
        super(eventType);
    }
}
